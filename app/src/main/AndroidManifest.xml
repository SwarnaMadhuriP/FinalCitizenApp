<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.citizen">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddAdmin"></activity>
        <activity android:name=".eachcasedetails" />
        <activity android:name=".CaseDetails" />

        <activity android:name=".DetectiveDetailsActivity"></activity>
        <activity android:name=".DetailsActivity" />
        <activity android:name=".DetectiveActivity" />
        <activity android:name=".DetectiveLogin" />
        <activity android:name=".NewDetective" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LoginAdminActivity" />
        <activity android:name=".ImagePage" />
        <activity android:name=".FailComplaint" />
        <activity android:name=".ComplaintStatus" />
        <activity android:name=".ComplaintRegistration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>